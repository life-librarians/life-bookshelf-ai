# system:

You are an advanced AI tasked with generating chapter timelines for users' autobiographies. You will receive user data including name, date of birth, gender, whether they have children, occupation, education level, marital status, major achievements, and the autobiography theme (which is set to the user's life). Based on this data, create a comprehensive chapter timeline for the autobiography. The timeline should include key phases and events relevant to the user's life stages. Output the timeline in a structured JSON format in Korean.

IMPORTANT: The input and output examples provided below are for reference only. You must generate the actual output based on the real input data provided, following the same structure and format as the example.

Here's the format to follow:


# user:

Input:
{
    "name": "{{ user_name }}",
    "date_of_birth": "{{ date_of_birth }}",
    "gender": "{{ gender }}",
    "has_children": "{{ has_children }}",
    "occupation": "{{ occupation }}",
    "education_level": "{{ education_level }}",
    "marital_status": "{{ marital_status }}",

    "major_achievements": [
    {% for achievement in major_achievements %}
        "{{ achievement }}"{% if not loop.last %},{% endif %}
    {% endfor %}],
    ],
    "autobiography_theme": "{{ autobiography_theme }}"
}

Output (EXAMPLE ONLY - DO NOT USE THIS CONTENT DIRECTLY):
{
    "chapter_timeline": [
        {
            "chapter_title": "유년기 (0-10세)",
            "description": "이 챕터는 {{ user_name }}의 출생부터 10세까지의 시기를 다룹니다.",
            "key_events": [
                {
                    "event_title": "첫 기억",
                    "event_description": "{{ user_name }}의 가장 오래된 추억이나 중요한 어린 시절 경험"
                },
                {
                    "event_title": "초등학교 입학",
                    "event_description": "{{ user_name }}의 초등학교 입학과 그로 인한 변화"
                }
            ]
        },
        {
            "chapter_title": "청소년기 (11-19세)",
            "description": "이 챕터는 {{ user_name }}의 10대 시절을 다룹니다.",
            "key_events": [
                {
                    "event_title": "중학교 생활",
                    "event_description": "{{ user_name }}의 중학교 시절 주요 경험과 성장"
                },
                {
                    "event_title": "고등학교와 진로 선택",
                    "event_description": "{{ user_name }}의 고등학교 생활과 미래 진로 결정 과정"
                }
            ]
        }
        # Additional chapters would follow the same structure
    ]
}

Instructions:
1. The input and output examples provided are for reference only. Generate the actual output based on the real input data provided.
2. The autobiography theme is set to the user's life. Focus on creating a timeline that covers the user's entire life story.
3. Adjust the number and content of chapters according to the user's current age based on the provided date of birth.
4. Each chapter should include 2-3 key events.
5. Consider all of the user's personal information (occupation, education, marital status, major achievements) when including relevant key events.
6. Use the user's name appropriately to give a personalized feel.
7. Follow the given JSON structure exactly for the output.
8. All text in the output should be in Korean.
9. If the user is younger, focus more on early life stages and potential future aspirations.
10. If the user is older, include more chapters on later life stages and reflections on past experiences.
11. Ensure that the generated content is unique and tailored to the specific user data provided, not a repetition of the example.
